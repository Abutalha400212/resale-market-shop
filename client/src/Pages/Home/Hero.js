import React from "react";
import Carousel from "../../Components/Carousel/Carousel";

const Hero = () => {
  return (
    <div className="relative">
      <Carousel />
    </div>
  );
};

export default Hero;
